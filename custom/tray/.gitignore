# Windows version resource, generated by custom/build-windows.ps1 from
# branding.ps1 and removed again after the build. Committing it would freeze a
# version number into the tree.
resource.syso
versioninfo.json
